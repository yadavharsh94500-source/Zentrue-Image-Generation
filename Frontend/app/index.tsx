import HomeScreen from "@/src/screens/HomeScreen";

// index.tsx = app entry point via Expo Router
// All logic lives in src/screens/HomeScreen
export default function Index() {
  return <HomeScreen />;
}